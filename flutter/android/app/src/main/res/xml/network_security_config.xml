<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is DENIED by default. Production must use HTTPS
  (Stage F1 sections 42 and 43). The exceptions below exist only so
  `flutter run` works against a local development backend:

    10.0.2.2   the Android emulator alias for the host machine
    localhost  and 127.0.0.1 for USB reverse tethered dev

  To test on a PHYSICAL device against a plain HTTP backend on your LAN,
  add that host IP below as another domain entry, or (preferred) put the
  API behind HTTPS and pass it with
  dart-define API_BASE_URL set to an https URL.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
